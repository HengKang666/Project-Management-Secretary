# ===== 公开仓库策略 =====
# 入库：代码 + README + docs/ + skills/ + .env.example
# 不入库：凭据、业务知识库、数据与产物

# 凭据与本机环境
.env
*.env
.env.*
!.env.example
.credentials.yaml

# 文档默认不收，只收 docs/ 与 skills/ 与 deploy/
*.md
!README.md
!docs/**
!skills/**
!deploy/**

# 以下几类放在最后，优先级最高，即使在上面目录里也不收
secretary/电力业务口径文档.md
secretary/数据地图.md
secretary/口径库.md
secretary/metric_catalog.json
docs/历史/构建说明.md

# 数据与产物
output/
.dsh/

# 运行日志（本机起服务时生成，不入库）
*.log

# 纠错引擎的词典：catalog.csv / area_core.csv 含两万个真实台区名录，属业务数据
# 缺了它纠错功能会自动降级（不报错），需要时单独获取并放回 data/ 目录
secretary/libs/name_correction_lib/data/

# Python
__pycache__/
*.pyc

# 编辑器 / 系统
.vscode/
.idea/
Thumbs.db
Desktop.ini
