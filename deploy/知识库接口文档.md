# 知识库接口 · 对接说明（v1.1）

> 本文只讲**知识库**这一组接口，是《接口对接说明.md》（智能问数）的**姊妹篇**，两者互不影响。
> 服务地址与问数服务**同一个**：本机 `http://127.0.0.1:8200`，生产 `http://47.121.183.81:8200`
> （**部署与运维**见第十节；本文示例统一用 `127.0.0.1`，是因为它们都在服务器本机执行 ——
> 换成 `47.121.183.81` 结果完全一样）。

---

## 一、这是什么

把阿里云百炼（Model Studio）的**知识库管理**能力接进了本服务，共 **5 个操作 + 1 个自检**：

| 想做什么 | 接口 | 谁用 |
|---|---|---|
| 看有哪些知识库 | `GET /api/kb/indices` | 同事（前端页）|
| 看某个知识库的详情 | `GET /api/kb/indices/{index_id}` | 同事（前端页）|
| 看知识库里有哪些文档 | `GET /api/kb/indices/{index_id}/documents` | 同事（前端页）|
| 传一份文档进知识库 | `POST /api/kb/indices/{index_id}/documents` | 同事（前端页）|
| 从知识库里删掉一份文档 | `DELETE /api/kb/indices/{index_id}/documents/{file_id}` | 同事（前端页）|
| 检查云端配好没有 | `GET /api/kb/health` | 排障用 |

**两个关键前提，先说清：**

1. **知识库在阿里云那边，服务只是"遥控器"。** 上传的文档实际存在阿里云百炼，本服务不保存任何知识库内容。删掉了本地也找不回来。
2. **知识库是"附属能力"，坏了不影响问数。** 凭据没配 / SDK 没装 / 连不上云端，只会让 `/api/kb/**` 返回 **503** 并说明原因；`/api/ask` 问答一切照常。

---

## 二、基本信息

| 项 | 值 |
|---|---|
| 服务地址 | `http://<服务器IP>:8200`（本机 `http://127.0.0.1:8200`）|
| 编码 | UTF-8 |
| 鉴权 | **无需 token**；阿里云 AK/SK 只存在服务端 `.env`，**前端拿不到** |
| 前端页面 | `http://<服务器IP>:8200/kb`（知识库控制台，给同事直接点着用）|
| 数据格式 | 请求/响应均为 `application/json`（上传除外）|

### 服务端要先配好 4 个值

在项目根目录的 `.env` 里（键名见 `.env.example`）：

```ini
ALIBABA_CLOUD_ACCESS_KEY_ID=          # RAM 用户的 AccessKey
ALIBABA_CLOUD_ACCESS_KEY_SECRET=
WORKSPACE_ID=llm-3z7uw7fwz0vexxxx     # 业务空间 ID，**必须以 llm- 开头**
DEFAULT_INDEX_ID=o95n15b77j           # 默认知识库 ID（可选，配了就能省略路径里的 index_id）
```

> ⚠️ **`.env` 只在服务启动时读一次，改完必须重启服务。**
> 配好之后跑一次 `GET /api/kb/health`，它会直接告诉你卡在哪一层（见第七节）。

---

## 三、★ 最容易卡住的地方：两套权限体系

**阿里云 RAM 权限 ≠ 百炼业务空间成员，是两套完全独立的体系。**

| 体系 | 在哪配 | 管什么 |
|---|---|---|
| RAM 权限 | 阿里云控制台 → 访问控制 | 能不能调开放接口（`AliyunBailianDataFullAccess`）|
| **业务空间成员** | **百炼控制台 → 左下角【权限管理】→【用户管理】** | 能不能访问**某个具体业务空间** |

**只配了前者、没配后者的典型症状（极具迷惑性）：**

```json
// GET /api/kb/health 返回 200，但……
{"ok": false, "credential_ok": true, "workspace_access_ok": false,
 "index_total": 0, "diagnosis": "workspace_forbidden",
 "hint": "..."}
```

紧接着所有写操作报 `NOT AUTHORIZED` / `Access denied: ... or the workspace does not exist.`

**为什么会这样**：`ListIndices` 是百炼里少数**不校验业务空间成员身份**的接口，
所以它会"成功"返回空列表，把人误导成"知识库 ID 填错了"。
`/api/kb/health` 特意用 `ListCategory` 做了第二次探针，才能把这两种情况分开。

**修复**：主账号登录百炼控制台 → 左下角【权限管理】→【用户管理】→【新增用户】→
类型选「RAM 用户」→ 选中子账号 → 保存并配置权限 → 勾选目标业务空间，角色「管理员」。

> 2025-08-06 起百炼收回了子账号的默认高级权限，必须显式赋权。
> 最快的二分定位法：临时换**主账号** AK/SK 试一次（官方明确主账号可直接调用、无须加入业务空间）——
> 通了就是子账号权限问题，不通就是 `WORKSPACE_ID` 填错了。

---

## 四、接口总览

| # | 方法 | 路径 | 说明 |
|---|---|---|---|
| 1 | GET | `/api/kb/health` | 分层自检：凭证是否有效 / 业务空间是否有权 |
| 2 | GET | `/api/kb/indices` | 知识库列表 |
| 3 | GET | `/api/kb/indices/{index_id}` | 知识库详情（配置 + 各状态文档数 + 存储监控）|
| 4 | GET | `/api/kb/indices/{index_id}/documents` | 知识库里的文档列表 |
| 5 | POST | `/api/kb/indices/{index_id}/documents` | 上传文档（multipart 或裸二进制）|
| 6 | DELETE | `/api/kb/indices/{index_id}/documents/{file_id}` | 删除知识库里的文档 |

> `{index_id}` 可以**省略**：路径写成 `/api/kb/indices//documents` 不美观，
> 所以当知识库只有一个时，直接配 `.env` 的 `DEFAULT_INDEX_ID`，路径里仍传具体 ID 更清晰。

---

## 五、逐个接口

### 1. 连通性自检 `GET /api/kb/health`

排障**第一个该跑的接口**。不消耗配额、不产生数据。

**响应**

```json
{
  "ok": true,                       // 一站式结论：凭证+空间权都通
  "credential_ok": true,            // AK/SK 能通过鉴权
  "workspace_access_ok": true,      // 有该业务空间的访问权
  "workspace_id": "llm-3z7uw7fwz0vexxxx",
  "endpoint": "bailian.cn-beijing.aliyuncs.com",
  "index_total": 2,                 // 该业务空间下的知识库数量
  "indices": [ { "index_id": "...", "name": "..." } ],
  "diagnosis": "healthy",           // 见下表
  "hint": ""                        // 出问题时给下一步该做什么
}
```

`diagnosis` 取值对照：

| 值 | 含义 | 下一步 |
|---|---|---|
| `healthy` | 一切正常 | — |
| `no_indices` | 凭证/权限都对，但空间下没有知识库 | 去百炼控制台建一个 |
| `workspace_forbidden` | **没加入该业务空间** | 见第三节 |
| `credentials_invalid` | AK/SK 本身不对 | 检查是否复制完整、是否与账号一致 |
| `transient_error` | 云端临时故障 | 稍后重试 |

```bash
curl "http://127.0.0.1:8200/api/kb/health"
```

---

### 2. 查看知识库 `GET /api/kb/indices`

**Query 参数**

| 参数 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `name` | string | — | 按知识库名称**精确**查找 |
| `page_number` | int | 1 | 页码 |
| `page_size` | int | 20 | 每页条数（1~100）|

**响应**

```json
{
  "total_count": 2,
  "page_number": 1,
  "page_size": 20,
  "indices": [
    {
      "index_id": "o95n15b77j",
      "name": "随州电力业务知识库",
      "description": "……",
      "structure_type": "unstructured",
      "source_type": "DATA_CENTER_CATEGORY",
      "embedding_model": "text-embedding-v4",
      "rerank_model": "gte-rerank",
      "rerank_min_score": 0.2,
      "chunk_size": 500,
      "overlap_size": 50,
      "separator": "\n",
      "enable_rewrite": true
    }
  ]
}
```

```bash
curl "http://127.0.0.1:8200/api/kb/indices?page_size=50"
```

---

### 3. 知识库详情 `GET /api/kb/indices/{index_id}`

**Query 参数**

| 参数 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `with_monitor` | bool | true | 是否附带存储/检索监控（失败不影响整体）|

**响应** = 上面列表里的那个对象 **+ 三项**：

```json
{
  "index_id": "o95n15b77j",
  "name": "随州电力业务知识库",
  "embedding_model": "text-embedding-v4",
  "chunk_size": 500,
  "...": "……同列表字段……",

  "document_total": 12,                                  // 文档总数
  "document_stats": {                                    // 按状态分组
    "finish": 10, "running": 1, "insert_error": 1, "deleted": 0
  },
  "monitor": { "storage": {...}, "retrieve": {...} }      // 存储用量 / 检索 QPS
}
```

> `document_stats` 里某项为 **-1** 表示该项统计失败，**不计入** `document_total`。
> 百炼**没有** GetIndex 接口，"详情"是服务端组合出来的（列表翻页找到配置 + 按状态统计 + 监控）。

```bash
curl "http://127.0.0.1:8200/api/kb/indices/o95n15b77j"
```

---

### 4. 知识库里的文档 `GET /api/kb/indices/{index_id}/documents`

**Query 参数**

| 参数 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `status` | string | — | `FINISH` / `RUNNING` / `INSERT_ERROR` / `DELETED` / `PARSE_FAILED` / `DOC_PARSING` |
| `name` | string | — | 按文件名（**不含后缀**）过滤 |
| `name_like` | bool | false | `name` 是否按模糊匹配 |
| `page_number` | int | 1 | 页码 |
| `page_size` | int | 20 | 每页条数（1~100）|

**响应**

```json
{
  "total_count": 12,
  "page_number": 1,
  "page_size": 20,
  "documents": [
    {
      "file_id": "file_9a657325...",   // ★ 删除时要用它
      "name": "供电所名称对照表.pdf",
      "size": 204800,                   // 字节
      "document_type": "PDF",
      "status": "FINISH",               // FINISH=已可检索；RUNNING=索引构建中
      "code": "",
      "message": "",
      "gmt_modified": "2026-09-21 10:12:00",
      "category_id": "default"
    }
  ]
}
```

**状态含义（前端要用颜色区分）**

| status | 含义 | 用户该怎么做 |
|---|---|---|
| `FINISH` | 已导入，**可被检索** | 正常 |
| `RUNNING` | 索引构建中（异步） | 等，刷新即可；**别重复上传** |
| `INSERT_ERROR` | 导入失败（看 `message`）| 删除后重新上传 |
| `PARSE_FAILED` | 云端解析失败 | 检查文件是否损坏/加密 |
| `DOC_PARSING` | 正在解析 | 等 |

```bash
curl "http://127.0.0.1:8200/api/kb/indices/o95n15b77j/documents?status=FINISH&page_size=50"
```

---

### 5. 上传文档 `POST /api/kb/indices/{index_id}/documents`

**两种请求格式，任选其一。**

#### 方式 A：multipart/form-data（标准，Postman / curl -F / 网页表单）

字段名随意（服务端取**第一个带 `filename` 的字段**）：

```bash
curl -F "file=@./供电所名称对照表.pdf" \
     "http://127.0.0.1:8200/api/kb/indices/o95n15b77j/documents"
```

#### 方式 B：裸二进制 + `X-Filename` 头（前端 `fetch` 最省事）

```js
await fetch(`/api/kb/indices/${id}/documents`, {
  method: 'POST',
  headers: { 'X-Filename': encodeURIComponent(file.name) },  // ★ 中文名要 URL-encode
  body: file,                                                // 直接传 File 对象
});
```

> **为什么中文文件名要 `encodeURIComponent`**：HTTP 头在 Python 里按 latin-1 解码，
> 直接塞中文会乱码。服务端会 `unquote` 还原，所以两种写法（编码/不编码）都能用。

**Query 参数（都有合理默认，一般不用传）**

| 参数 | 默认 | 说明 |
|---|---|---|
| `wait_parse` | true | 是否等云端解析完成再提交索引。`false` 则只上传登记，**不提交索引** |
| `submit_index` | true | 是否提交索引任务（需 `wait_parse=true`）|
| `skip_if_exists` | true | 上传前按**文件名**查一次，同名就跳过（尽力而为的去重）|

**响应**

```json
{
  "file_id": "file_9a657325...",
  "job_id": "index_xxx",              // 索引任务 ID；未提交时为 null
  "name": "供电所名称对照表.pdf",
  "size": 204800,
  "md5": "9a657325...",
  "parse_status": "PARSE_SUCCESS",    // 等解析超时会返回 PARSING（不算失败）
  "index_submitted": true,
  "skipped": false,                   // true = 同名已存在，本次没上传
  "message": "已提交索引任务。索引在云端异步构建，高峰期可能耗时数小时；……"
}
```

**上传做了什么（5 步链路，都是服务端自动完成）**

```
① ApplyFileUploadLease  申请上传租约（拿到预签名 URL）
② HTTP PUT              把文件二进制传到那个 URL
③ AddFile               登记到「数据连接」，拿到 fileId
④ DescribeFile 轮询     等云端解析完成（PARSE_SUCCESS）
⑤ SubmitIndexAddDocumentsJob  提交索引任务（★ 异步，可能耗时数小时）
```

**注意**

- 单文件 **1B ~ 100MB**。
- 第 ①⑤ 步的接口**都不幂等**：同一个文件重复提交会产生重复文档/重复切片。
  所以服务端做了同名检查（`skip_if_exists`），**但前端仍应防连点**。
- 返回 `skipped:true` 时表示知识库里已有同名文档，`file_id` 是**已有那份**的 ID。
- **索引是异步的**：返回 `index_submitted:true` 只是"任务交出去了"，
  要刷新文档列表看到 `FINISH` 才算真正可检索。

---

### 6. 删除知识库文档 `DELETE /api/kb/indices/{index_id}/documents/{file_id}`

```bash
curl -X DELETE "http://127.0.0.1:8200/api/kb/indices/o95n15b77j/documents/file_9a657325..."
```

**响应**

```json
{ "deleted": ["file_9a657325..."], "requested": "file_9a657325...",
  "note": "只删了知识库里的这份文档；「数据连接」里的源文件仍在，要彻底清理请到百炼控制台处理。" }
```

**三条规则，必须知道：**

1. **只有 `FINISH` / `INSERT_ERROR` 状态的文档能删**。`RUNNING`（索引构建中）删不掉，会报错。
2. **删除不可逆**，且**不会删除「数据连接」里的源文件**（那是另一个层）。
3. 云端**没有"更新文档"语义**：改了内容只能**先删再重传**。

---

## 六、同事怎么用（前端页面）

**打开 `http://<服务器IP>:8200/kb`**，页面上四块，对应上面 4 个操作：

| 页面区块 | 对应接口 | 怎么操作 |
|---|---|---|
| ① 查看知识库 | `GET /api/kb/indices` | 点「刷新列表」。点行里的「选用」会把该 ID 填到下面三个输入框 |
| ② 知识库详情 | `GET /api/kb/indices/{id}` | 填 ID 点「查看详情」，看文档数与各状态统计 |
| ③ 知识库里的文档 | `GET .../documents` | 填 ID、可选状态，点「刷新文档」；每条右侧有「删除」 |
| ④ 上传文档 | `POST .../documents` | 填 ID、选文件、点「上传」；上传完自动刷新文档列表 |
| （顶部）连通性自检 | `GET /api/kb/health` | 点「连通性自检」，右上角状态灯变绿/红 |

页面**打开时会自动做一次自检**，所以"没配凭据 / 权限不对"一眼就能看见，不用等点了上传才发现。

> **监听地址取决于"怎么起的"，不是取决于 `.env`：**
>
> | 启动方式 | 监听 | 同事能否访问 |
> |---|---|---|
> | 直接 `python server.py` | `127.0.0.1` | **不能**，只有本机 |
> | `bash start.sh` / `start.sh -d` | `0.0.0.0` | 能（脚本里设了 `SECRETARY_HOST`）|
> | `sudo bash install_service.sh`（服务器上的方式）| `0.0.0.0` | 能（systemd 单元里设了）|
>
> 服务器上走的是第三种，所以同事直接访问 `http://47.121.183.81:8200/kb` 即可，
> **不需要改 `.env`**。真要改端口/监听地址，见第十节 10.4。

---

## 七、错误码对照

服务端会把云端错误**翻译成语义正确的 HTTP 状态码**（不会一律返 500）：

| 状态码 | 含义 | 典型原因 | 排查 |
|---|---|---|---|
| 200 | 成功 | — | — |
| 400 | 参数错 | 路径参数空、文件为空、超 100MB | 看响应里的 `error` |
| 403 | **鉴权/授权失败** | 没加入业务空间、AK/SK 不对、签名错 | **跑 `GET /api/kb/health`**，见第三节 |
| 404 | 找不到 | `index_id` / `file_id` 不存在 | 核对 ID；确认它属于当前业务空间 |
| 405 | 方法不支持 | 用错 HTTP 方法 | 看第四节 |
| 429 | 被限流 | 调用太频繁（如 `GetIndexJobStatus` 限 20 次/分）| 降低轮询频率，稍后重试 |
| 502 | **云端故障** | 百炼侧异常 | 稍后重试 |
| 503 | **本地没配好** | 缺 `.env` 凭据 / SDK 未安装 | 响应里直接写明了缺什么 |
| 504 | 调用云端超时 | 网络慢 / 云端卡 | 重试 |

**响应体两种形态都可能有，前端要都能认：**

```json
// A. 云端业务错误（HTTP 200 或 4xx，错误在 body）
{ "code": "Index.NotFound", "message": "……", "hint": "……", "retryable": false }

// B. 本地/参数错误
{ "error": "知识库未配置：缺少 XXX。请在项目根目录 .env 里补齐……" }
```

---

## 八、注意事项（踩过的坑）

1. **提交类接口不幂等** —— `ApplyFileUploadLease` / `SubmitIndexAddDocumentsJob` 重复调用会产生重复数据。前端要防连点。
2. **索引构建是异步的** —— "索引构建中"（`RUNNING`）是**正常状态**，高峰期可能耗时数小时；小文件通常几分钟到几十分钟。**不要因为看着没动静就重复提交。**
3. **删除有状态门槛** —— 只有 `FINISH` / `INSERT_ERROR` 能删。
4. **云端没有更新语义** —— 改文档只能"先删再传"，用 MD5 判断是否真的变了。
5. **只支持华北2（北京）** —— 中国站知识库功能仅该地域提供，接入点固定 `bailian.cn-beijing.aliyuncs.com`。
6. **`.env` 只在启动时读一次** —— 改完凭据必须重启服务。
7. **`WORKSPACE_ID` 与 `INDEX_ID` 别填反** —— 前者必须 `llm-` 开头。
8. **AK/SK 永远不下发前端** —— 所有调用都经过本服务中转。

---

## 九、调用示例

### curl 全流程

```bash
BASE=http://127.0.0.1:8200
ID=o95n15b77j

# 0) 先自检
curl "$BASE/api/kb/health"

# 1) 看有哪些知识库
curl "$BASE/api/kb/indices?page_size=50"

# 2) 看详情
curl "$BASE/api/kb/indices/$ID"

# 3) 看文档（只看已可检索的）
curl "$BASE/api/kb/indices/$ID/documents?status=FINISH&page_size=50"

# 4) 上传
curl -F "file=@./产品手册.pdf" "$BASE/api/kb/indices/$ID/documents"

# 5) 删除（file_id 从第 3 步响应里取）
curl -X DELETE "$BASE/api/kb/indices/$ID/documents/file_9a657325..."
```

### Python（完整流程：列表 → 上传 → 轮询状态 → 删除）

```python
import time
import requests

BASE = "http://127.0.0.1:8200"
INDEX = "o95n15b77j"

# 1) 知识库列表
for i in requests.get(f"{BASE}/api/kb/indices").json()["indices"]:
    print(i["index_id"], i["name"])

# 2) 上传（裸二进制 + X-Filename）
with open("产品手册.pdf", "rb") as f:
    r = requests.post(
        f"{BASE}/api/kb/indices/{INDEX}/documents",
        headers={"X-Filename": "产品手册.pdf"},
        data=f,
    ).json()
print("file_id =", r["file_id"], "| 索引任务 =", r["job_id"])

# 3) 轮询文档状态直到 FINISH（索引是异步的）
for _ in range(60):
    docs = requests.get(f"{BASE}/api/kb/indices/{INDEX}/documents?page_size=100").json()["documents"]
    me = [d for d in docs if d["file_id"] == r["file_id"]]
    if me and me[0]["status"] == "FINISH":
        print("已可检索")
        break
    time.sleep(10)

# 4) 删除
print(requests.delete(f"{BASE}/api/kb/indices/{INDEX}/documents/{r['file_id']}").json())
```

### Java（OkHttp）

```java
String base = "http://127.0.0.1:8200";
String id   = "o95n15b77j";
OkHttpClient http = new OkHttpClient();

// 列表
Request listReq = new Request.Builder().url(base + "/api/kb/indices").build();
System.out.println(http.newCall(listReq).execute().body().string());

// 上传（multipart）
RequestBody body = new MultipartBody.Builder().setType(MultipartBody.FORM)
        .addFormDataPart("file", "产品手册.pdf",
                RequestBody.create(new File("产品手册.pdf"), MediaType.parse("application/pdf")))
        .build();
Request upReq = new Request.Builder()
        .url(base + "/api/kb/indices/" + id + "/documents").post(body).build();
System.out.println(http.newCall(upReq).execute().body().string());

// 删除
Request delReq = new Request.Builder()
        .url(base + "/api/kb/indices/" + id + "/documents/file_9a657325...")
        .delete().build();
System.out.println(http.newCall(delReq).execute().body().string());
```

---

## 十、部署与环境（运维看）

> 这一节是**给部署和排障的人**的。只对接接口的同事看完第二节就可以跳到第九节。

### 10.1 当前生产环境

| 项 | 值 |
|---|---|
| 服务器 | 阿里云 ECS `47.121.183.81`（hostname `gjdw-suizhou-zhinengwenxiao`）|
| **服务地址** | **`http://47.121.183.81:8200`** |
| **知识库控制台** | **`http://47.121.183.81:8200/kb`** |
| 端口 | `8200`（可用 `SECRETARY_PORT` 覆盖）|
| 运行方式 | **systemd 常驻服务 `secretary`** —— 开机自启 + 崩溃 5 秒自动拉起 + 与 SSH 会话无关 |
| 部署目录 | `/root/agentkownlg/deploy_server/`（`.env` 与 `secretary/` 平级）|
| Python | 3.9+（服务只用标准库，见 10.2）|

> **★ 千万别用 `bash start.sh` 起生产服务**：那是前台进程，SSH 窗口一关 / 会话超时
> 就被 SIGHUP 杀掉，同事立刻调不通 —— 典型表现就是「昨天还能用，今天打不开」。

### 10.2 依赖装什么（分两档，理解这点能省很多排查时间）

```bash
cd /root/agentkownlg/deploy_server
python3 -m pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/
```

| 依赖 | 谁用 | 不装的后果 |
|---|---|---|
| `pymysql` | **问答主服务**（唯一必需的三方包）| 服务起不来 |
| `alibabacloud_bailian20231229`<br>`alibabacloud_tea_openapi` / `tea_util` / `requests` | **只有 `/api/kb/**` 用** | 问答**一切正常**，知识库接口返 **503** |

> **★ 这是刻意的设计**：`server.py` **不在顶层 import** 阿里云 SDK，第一次用到才 import。
> SDK 没装 / 装坏了，**只影响这一组接口，绝不拖垮问答**。
> 所以「问答能用但 `/api/kb/**` 全 503」时，**先怀疑依赖没装**，而不是网络或权限。

离线安装（服务器不能连外网 pip 时）：本地 `pip download -d whl -r requirements.txt`，
把 `whl/` 传上去 → `python3 -m pip install --no-index --find-links=whl -r requirements.txt`。

### 10.3 `.env` 放哪、改完怎么生效

| 项 | 说明 |
|---|---|
| 位置 | `<部署根目录>/.env`，**与 `secretary/` 平级**（不是放进 `secretary/`）|
| 权限 | `chmod 600 .env`（里面有 DB 密码和模型 Key）|
| 生效时机 | **只在服务启动时读一次** → 改完**必须重启**（见 10.4）|
| 必填 4 键 | `ALIBABA_CLOUD_ACCESS_KEY_ID` / `..._SECRET` / `WORKSPACE_ID` / `DEFAULT_INDEX_ID`（最后一个可选）|

```ini
ALIBABA_CLOUD_ACCESS_KEY_ID=
ALIBABA_CLOUD_ACCESS_KEY_SECRET=
WORKSPACE_ID=llm-3z7uw7fwz0vexxxx     # ★ 必须 llm- 开头
DEFAULT_INDEX_ID=o95n15b77j           # 可选：配了就能省略路径里的 index_id
```

> ⚠️ **`WORKSPACE_ID` 与 `INDEX_ID` 最常被填反** —— 前者 `llm-` 开头，后者是 8 位短 ID。
> 填反的典型症状是 `/api/kb/health` 里 `index_total: 0` + `workspace_forbidden`（见第三节）。

### 10.4 起 / 停 / 重启 / 看日志

| 场景 | 命令 |
|---|---|
| 改完代码或 `.env` 后生效 | `sudo systemctl restart secretary` |
| 看状态 | `systemctl status secretary` |
| 看实时日志 | `journalctl -u secretary -f` |
| 看程序输出日志 | `tail -f /root/agentkownlg/deploy_server/server.log` |
| 停止 | `sudo systemctl stop secretary` |
| 卸载（停服 + 取消开机自启）| `sudo bash install_service.sh uninstall` |

**首次部署或单元文件路径不对时**，用脚本一键重装（会自己生成正确的 unit）：

```bash
cd /root/agentkownlg/deploy_server
chmod +x start.sh install_service.sh
sudo bash install_service.sh
```

> ⚠️ **停止服务时必须 `systemctl stop`，不能只 `pkill -f server.py`** ——
> 单元里配了 `Restart=always`，只 pkill 的话 systemd 会在 5 秒内把进程**重新拉起来**，
> 看起来就是"停不掉"。两个都做才干净。

### 10.5 放行 8200（**最容易漏的一步**）

服务正常、同事却连不上，九成是这里。

```bash
# firewalld（CentOS / 龙蜥 / 红旗）
firewall-cmd --add-port=8200/tcp --permanent && firewall-cmd --reload
firewall-cmd --list-ports

# ufw（Ubuntu / Debian）
ufw allow 8200/tcp && ufw status

# 确认监听的是 0.0.0.0 而不是 127.0.0.1
ss -tlnp | grep 8200
```

**云服务器还要去控制台「安全组」放行 8200 入方向** —— 系统防火墙开了、安全组没开，一样连不上。

### 10.6 部署完的自检（三级，都要跑）

```bash
# ① 服务与附属能力 —— 四个字段都要 true
curl -s http://127.0.0.1:8200/health
# {"ok":true,"model":"qwen3.8-max","name_fix":true,"name_fix_source":"db:agent_data",
#  "qa_log":true,"kb":true,"kb_source":"aliyun-bailian"}

# ② 知识库云端连通（这一级才真的打阿里云）
curl -s http://127.0.0.1:8200/api/kb/health
# 期望 {"ok":true,"diagnosis":"healthy","index_total":8}

# ③ 页面与问数主链路回归
curl -s -o /dev/null -w "kb.html HTTP %{http_code}\n" http://127.0.0.1:8200/kb
```

| `/health` 字段 | `false` 说明什么 | 怎么修 |
|---|---|---|
| `ok` | 服务没起来 | 看 `journalctl -u secretary -n 50` |
| `name_fix` | 纠错词典没生效 | 词典 CSV 没随包传 / `agent_data` 的 6 张 `t_nc_*` 表连不上 |
| `qa_log` | 对话记录不落库 | `agent_data` 库连不上（**问答照常**，只是不留记录）|
| **`kb`** | 知识库能力没就绪 | 看 `kb_reason`：**缺配置** → 补 `.env`；**依赖未安装** → 10.2 |

> ★ **`kb:true` 只代表「凭据齐 + SDK 装了」，不代表云端能用。**
> 真正的连通性看 ② `/api/kb/health` —— 这是两件事，别混。

### 10.7 ★ 安全：知识库这组是**写接口**

**问数接口没鉴权只是"被白嫖查询"（数据库只读，风险可控）。
但 `/api/kb/**` 能传文档、删文档 —— 挂到公网 8200，任何能访问这个 IP 的人
都能往百炼知识库里塞文件、把文件删掉**，既污染知识库、又烧配额
（`ApplyFileUploadLease` / `SubmitIndexAddDocumentsJob` 都不幂等，重复调用会造重复切片）。

**至少选一项做：**

- **推荐**：云控制台「安全组」把 8200 入方向**限制到公司出口 IP 段**（不用改代码）
- 或系统防火墙限定来源网段：
  ```bash
  firewall-cmd --permanent --add-rich-rule='rule family="ipv4" source address="<公司出口网段>/24" port port="8200" protocol="tcp" accept'
  firewall-cmd --reload
  ```
- 或给 `/api/kb/**` 加一层 token 校验（要改 `kb_api.py`）
- 或暂不向外发布 `/kb` 页面地址

> AK/SK 本身是安全的：只存在服务端 `.env`，**所有调用都经本服务中转，前端拿不到**。

### 10.8 边界与容量

- 单文件 **1B ~ 100MB**；服务是**单进程多线程**（Python 标准库 `http.server`），
  适合几个人用，**不适合当高并发网关**。
- 服务**没有登录校验**，也**没有限流** —— 对外发布务必配合 10.7。
- 索引构建是**云端异步**的，高峰期可能耗时数小时，与本地部署无关，重启服务不影响云端任务。

### 10.9 ★ 两个「知识库」不要混：**本页管的是「管理面」，问数用的是「检索面」**

系统里其实牵着**两组互不相通**的知识库配置，最容易犯的错是：
**在 `/kb` 页面把文档传进了 A 库，然后发现问数答话一点没变。**

| | **管理面（本页这组接口）** | **检索面（问数/补全在用的）** |
|---|---|---|
| 谁在用 | `/api/kb/**`、`/kb` 页面 | 补全 `agent.complete_question()`、问答里的 `kb_search` 工具 |
| 读哪个配置 | `.env` 的 `WORKSPACE_ID` + **`DEFAULT_INDEX_ID`** | `config.py` 的 **`KB_AGENT_ID`（检索服务）+ `KB_IDS`（限定库）** |
| 当前值 | `DEFAULT_INDEX_ID=igjhr8giyb`（ai大脑业务知识库）| `KB_AGENT_ID=aid-72fa8…`，`KB_IDS=r57xtq9ypm`（ai大脑通用语义知识库）|
| 干什么 | 列出/上传/删除**云端文档** | 按语义检出**文档切片**塞给模型 |
| 走哪条链路 | 阿里云百炼**开放接口**（AK/SK 签名）| maas **知识检索服务**（用 `LLM_API_KEY` 作 Bearer）|

**⇒ 在 `igjhr8giyb` 里传文档，不会影响问数** —— 因为检索走的是 `r57xtq9ypm`，
而当前检索服务**也没被授权**读 `igjhr8giyb`（实测传该 ID 直接 `401 Unauthorized`，见 10.10）。

**想让「传进去的东西立刻影响问数」，有两条路：**

1. 把文档传进**检索正在用的那个库**（把 `DEFAULT_INDEX_ID` 改成 `r57xtq9ypm`）
2. 或反过来：**把检索切到你传文档的那个库**（见 10.10，需要先在百炼控制台授权）

### 10.10 换检索知识库：改代码没用，要先过「授权」这一关

**实测（2026-09-21，线上真实调用）：**

```
检索服务 aid-72fa… + r57xtq9ypm   → total=1  《预算与成效分析AI业务知识库》
检索服务 aid-72fa… + uxht00z9ey   → 401 Unauthorized      ← 只改 KB_IDS 不生效
检索服务 aid-72fa… + igjhr8giyb   → 401 Unauthorized      ← 只改 KB_IDS 不生效
检索服务 aid-c345… + uxht00z9ey   → total=5  电力业务口径文档 / 口径库 / 数据地图
```

**结论：`KB_IDS` 只是「在检索服务已授权的范围内再收窄」，不是「选库」。**
检索服务（`aid-…`）在百炼控制台有自己的**绑定范围**，超出范围一律 401。

**换库的三步（缺一不可）：**

| 步 | 在哪做 | 做什么 |
|---|---|---|
| ① | 百炼控制台 → 知识检索服务 | 把目标知识库**加进检索服务的绑定范围**（或新建一个检索服务只绑它），拿到新的 `aid-…` |
| ② | 百炼控制台 → 权限管理 | 确认当前 AK/SK（或 `LLM_API_KEY` 对应的账号）对该库**有读权限** |
| ③ | **服务端 `config.py`** | 改 `KB_AGENT_ID`（换服务）和/或 `KB_IDS`（收窄范围），重启服务 |

> ⚠️ **`SECRETARY_KB_AGENT` / `SECRETARY_KB_IDS` 写在 `.env` 里不生效！**
> `config.py` 用的是 `os.environ.get()`，`.env` 只被读进 `ENV` dict、**没有写回环境变量**
> （同一坑也适用于 `SECRETARY_MODEL` / `SECRETARY_TABLES` / `SECRETARY_MAX_STEPS`）。
> 想让 `.env` 生效，只能二选一：**改 `config.py` 里的默认值**，或
> **加真环境变量**（systemd 单元里加 `Environment=SECRETARY_KB_IDS=…`）。
> 例外：`SECRETARY_LEXICON*` 和 4 个百炼键是做了 `.env` 兜底的，**它们 `.env` 生效**。

**另一种更省事的换库方式**：不碰检索服务，直接把新文档**传进检索正在用的那个库**
（即把 `.env` 的 `DEFAULT_INDEX_ID` 改成 `r57xtq9ypm`，用 `/kb` 页面上传）——
因为检索服务已经绑了它，传完就能被检到。

---

## 附：文件清单（本包都有什么）

```
deploy_server4.0/
├── .env                  ← 4 个百炼键 + DB/模型凭据（含密码，别传到公开位置）
├── .env.example          ← 配置项说明
├── requirements.txt      ← 依赖清单（pymysql + 阿里云 SDK + requests）
├── start.sh / start.bat  ← 临时启动脚本（生产别用，见 10.4）
├── install_service.sh    ← 一键装 systemd 常驻服务
├── DEPLOY.md             ← 主服务部署说明
├── 接口对接说明.md         ← 智能问数接口（姊妹篇）
├── 知识库接口文档.md       ← 本文
└── secretary/
    ├── server.py         ← 入口：只加了「把 /api/kb/** 转给 kb_api」
    ├── kb_api.py         ← ★ 知识库 HTTP 接口层（路由 / 错误码 / multipart 解析）
    ├── kb_bailian.py     ← ★ 云端封装层（唯一接触阿里云 SDK 的地方）
    ├── kb_config.py      ← 知识库配置（读项目根 .env）
    ├── static/kb.html    ← 知识库控制台页面（同事用）
    └── …                 ← 问数主链路（agent / qa_log / name_fix / tools_* 等）
```

**分层的好处**：将来要换云（或换自建 RAG），**只改 `kb_bailian.py` 一个文件**；
`server.py`、`kb_api.py` 和前端完全不用动。

**升级 / 回滚的完整命令清单**见工作区根目录《服务器升级操作手册_4.0.md》。
